const { list1, list2 } = require("./example");

console.log(list1);
console.log(list2);

// some standard modules available in node
const os = require("os");

console.log(os.platform);
console.log(os.homedir());
